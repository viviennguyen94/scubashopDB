<% include partials/header %>


<div class="container">
    <h1>Edit Page!</h1>
    <% if (message) { %>
        <p class="text-center text-danger"><%= message %></p>
    <% } %>

    <% if (product) { %>
        <form class="add-product-form" action="" method="post" enctype="multipart/form-data">
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label for="product-name">Product Name</label>
                    <input type="text" class="form-control" name="product_name" id="product-name" value="<%= product.product_name %>" required>
                </div>
                <div class="form-group col-md-6">
                    <label for="qty-in-stock">Quantity on Hand</label>
                    <input type="number" class="form-control" name="qty_in_stock" id="qty-in-stock" placeholder="Quantity" value="<%= product.qty_in_stock %>" required>
                </div>
                
            </div>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label for="price">Price</label>
                    <input type="number" class="form-control" name="price" id="price" placeholder="Price" value="<%= product.price %>" required>
                </div>
            </div>
            <button type="submit" class="btn btn-success float-right">Update Product</button>
        </form>
    <% } else { %>
        <p class="text-center">Product Not Found. Go <a href="/add">here</a> to add products.</p>
    <% } %>
</div>
<% include partials/footer %>