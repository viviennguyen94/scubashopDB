<% include partials/header %>

<div class="container">
    <h1>Admin Page!</h1>

    <p>To add product to the database, click <a href="/add">here</a></p>
    <p>To edit or delete a product, click <a href="/admin">here</a></p>
    <p>To view order shipping details, click <a href="/orderShippingStatus">here</a></p>
    <div class="table-wrapper">
        <% if (orders.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">Order ID</th>
                        <th scope="col">Customer ID</th>
                        <th scope="col">Order Date</th>
                        <th scope="col">Card Number</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach((order, index) => { %>
                        <tr>
                            <th scope="row"><%= order.orderID %></th>
                            <td><%= order.cid %></td>
                            <td><%= order.order_date %></td>
                            <td><%= order.card_num %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No orders found.</p>
        <% } %>
    </div>
</div>

<!-- <a href="/campgrounds">View All Campgrounds</a> -->
<% include partials/footer %>